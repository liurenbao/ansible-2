apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: rms-ui
  namespace: rms
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/rms/`)
      kind: Rule
      services:
        - name: rms
          port: 80