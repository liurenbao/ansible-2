apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "workflow.fullname" . }}-test-connection"
  labels:
    {{- include "workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
