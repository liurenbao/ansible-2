apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rtd-web.fullname" . }}-test-connection"
  labels:
    {{- include "rtd-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rtd-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
