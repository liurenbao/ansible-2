apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtd-web.fullname" . }}
  labels:
    {{- include "rtd-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rtd-web.selectorLabels" . | nindent 4 }}
