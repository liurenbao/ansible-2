apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ruleengine.fullname" . }}-test-connection"
  labels:
    {{- include "ruleengine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ruleengine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
