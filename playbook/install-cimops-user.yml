- name: add cimops user
  hosts: all
  gather_facts: false
  tasks:
    - name: create cimops user
      user:
        name: '{{ cim_ops_user }}'
        password: '{{ cim_ops_user_pass }}'
        state: present